/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false, // Set to false to avoid DnD duplicate drag events during React dev strict mode
  output: 'standalone',
};

export default nextConfig;
